<%= form_with(model: real_estate_deal) do |form| %>
  <% if real_estate_deal.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(real_estate_deal.errors.count, "error") %> prohibited this real_estate_deal from being saved:</h2>

      <ul>
        <% real_estate_deal.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div style="margin-top: 20px">
    <%= form.submit %>
  </div>
<% end %>
